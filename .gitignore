node_modules/
.next/
dist/
build/
coverage/

.env
.env.local
.claude/settings.local.json

uploads/
corpus/chunks/*.vectors.json
fixtures/generated/

.DS_Store
*.log

__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
api/data/

# Terraform (deploy/terraform): local state and per-person values never belong
# in git. terraform.tfvars.example and .terraform.lock.hcl (pins provider
# versions) stay tracked; real .tfvars files and local state do not.
.terraform/
terraform.tfstate
terraform.tfstate.backup
deploy/terraform/*.tfvars
