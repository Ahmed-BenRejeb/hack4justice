# Excludes for the demo-deploy rsync (docs/deploy.md). Mirrors .gitignore
# plus the local Terraform state, which never needs to leave this machine.
#
# api/.env and web/.env are deliberately NOT excluded: the instance needs
# them to run, and they never touch git (see .gitignore). rsync is the
# only place they are copied.
.git/
node_modules/
.next/
dist/
build/
coverage/
uploads/
corpus/chunks/*.vectors.json
fixtures/generated/
__pycache__/
*.pyc
.venv/
.pytest_cache/
.ruff_cache/
api/data/
deploy/terraform/.terraform/
deploy/terraform/terraform.tfstate
deploy/terraform/terraform.tfstate.backup
deploy/terraform/*.tfvars
